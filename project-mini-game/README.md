# project-mini-game

Commit **your** Capital Syndicate game into **Tah’s** Board of Realities.

This Cloud Agent cannot clone or push to https://github.com/Tah-KMTS/Final-Project-board-of-realities (private). Run this **on your laptop** after Tah adds `PRangsi1886` as a **Write** collaborator:

```bash
./project-mini-game/commit-to-tah.sh
```

That copies `minigames/capital-syndicate/` into Tah’s repo, commits, and pushes to `main`.

Target path:

```text
Final-Project-board-of-realities/minigames/capital-syndicate/
```
